# Minimal requirements for the scaffold
SpeechRecognition
colorama
requests
gtts
deep-translator
langdetect
PyAudio
# Optional: pyaudio (voice input) - fallback to typed input when missing
pyautogui
pygetwindow
pywin32
plyer
speedtest-cli
keyboard
pynput
pystray
pillow
rapidfuzz
pywinauto
scikit-learn
joblib
psutil
selenium
faster-whisper
indic-transliteration
feedparser
pyglet
spacy
pywhatkit
wikipedia
# Optional: yt-dlp — for direct YouTube play (first result opens as watch URL)
# pip install yt-dlp

# Optional: pydub — for story TTS speed-up (faster playback). mp3 needs ffmpeg in PATH.
pydub

# ========= PROFESSIONAL AUDIO PROCESSING =========
# For Zoom/Teams-quality voice recognition
scipy                    # Signal processing, filters
numpy                    # Array operations (already implicit dep)
webrtcvad                # WebRTC Voice Activity Detection
# noisereduce            # Optional: ML-based noise reduction (heavy)
# torch                  # Optional: For Silero VAD (heavy, ~2GB)






